function Footer({ companyName }) {
  return (
    <footer className="footer">
      <p>Employee Directory — React Fundamentals Assignment</p>
      <p>{companyName}</p>
    </footer>
  );
}

export default Footer;